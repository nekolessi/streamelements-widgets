{
  "name": "streamelements-widgets",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "eslint .",
    "lint:fix": "eslint . --ext .js,.mjs --fix",
    "release": "changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@eslint/js": "^9.33.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.0.0",
    "eslint-plugin-import": "^2.29.1",
    "globals": "^15.0.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "vite": "^7.1.3"
  }
}
